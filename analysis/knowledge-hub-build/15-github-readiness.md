# 15 — GitHub Readiness

Repository initialized on `main`. Dependency lockfile exists. QA scripts are deterministic: `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build`, `npm run qa`.

Ready: source boundaries, ignore rules, reproducible install, prerendered output, sitemap/robots, static-host redirects, tests and analysis record.

Not supplied: GitHub remote, Lovable project connection, protected branch settings, CI workflow, staging URL, deployment platform status-rule configuration or credentials. No commit, push or deployment is performed without those inputs and a final content/legal release gate.

