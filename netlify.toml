[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  VITE_SITE_URL = "https://mpudeincoach-wissen.de"

[context.deploy-preview]
  command = "npm run build"

[context.branch-deploy]
  command = "npm run build"
